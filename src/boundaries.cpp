struct position {
  int x;
  int y;
};

struct boundaries {
  int x;
  int y;
};

void hasHitABoundaries(position Position, boundaries Boundaries) {
  int xBoundary = Boundaries.x;
  int yBoundary = Boundaries.y;

  int xPosition = Position.x;
  int yPosition = Position.y;
};
